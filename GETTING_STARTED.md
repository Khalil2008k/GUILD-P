# 🎯 البدء مع المشروع | Getting Started

## 🌟 مرحباً بك!

هذا دليل شامل خطوة بخطوة لإعداد وتشغيل مشروع العرض التقديمي العربي المتحرك.

---

## 📋 المتطلبات الأساسية

قبل البدء، تأكد من تثبيت:

- ✅ **Node.js** (النسخة 18.0 أو أحدث)
  - تحميل من: [nodejs.org](https://nodejs.org)
  - للتحقق: `node --version`

- ✅ **npm** (يأتي مع Node.js)
  - للتحقق: `npm --version`

- ✅ **محرر نصوص** (مثل VS Code)
  - تحميل من: [code.visualstudio.com](https://code.visualstudio.com)

---

## 🚀 الإعداد السريع (5 دقائق)

### الخطوة 1: افتح Terminal/PowerShell

**على Windows:**
```bash
# اضغط Windows + R
# اكتب: powershell
# اضغط Enter
```

**على Mac/Linux:**
```bash
# اضغط Cmd + Space (Mac) أو Ctrl + Alt + T (Linux)
# اكتب: terminal
```

### الخطوة 2: انتقل لمجلد المشروع

```bash
cd C:\Users\Admin\presentation
# أو المسار الذي يوجد فيه المشروع
```

### الخطوة 3: تحقق من الملفات

```bash
npm run verify
```

يجب أن ترى: ✅ للملفات الموجودة

### الخطوة 4: تثبيت التبعيات

```bash
npm install
```

⏱️ سيستغرق 2-3 دقائق في المرة الأولى

### الخطوة 5: تشغيل المشروع

```bash
npm run dev
```

### الخطوة 6: افتح في المتصفح

افتح: **http://localhost:3000**

🎉 **مبروك! المشروع يعمل الآن!**

---

## 📱 اختبار المشروع

### ماذا يجب أن ترى؟

1. **الشريحة الأولى (المقدمة)**:
   - أيقونة متحركة ✨
   - عنوان "المقدمة"
   - نص ترحيبي
   - رسالة "اضغط ← أو مرر للمتابعة"

2. **عناصر التنقل**:
   - نقاط على اليسار (1-10)
   - سهم تنقل على اليسار
   - رقم الشريحة أسفل اليسار

### اختبر التنقل:

- اضغط سهم **←** على لوحة المفاتيح
- أو اضغط **سهم التنقل الأيسر**
- أو اضغط على **النقاط الجانبية**

يجب أن تنتقل للشريحة الثانية بسلاسة ✅

---

## 🔧 حل المشاكل الشائعة

### المشكلة 1: `npm` ليس معروفاً

**السبب:** Node.js غير مثبت أو غير موجود في PATH

**الحل:**
1. حمّل Node.js من [nodejs.org](https://nodejs.org)
2. ثبّته (اختر "Add to PATH")
3. أعد فتح Terminal
4. تحقق: `node --version`

---

### المشكلة 2: الخطأ `EACCES` أو `permission denied`

**على Mac/Linux:**
```bash
sudo npm install
```

**على Windows:**
- شغّل PowerShell كمسؤول (Run as Administrator)

---

### المشكلة 3: المنفذ 3000 مستخدم

**الخطأ:** `Port 3000 is already in use`

**الحل:**
```bash
# أوقف العملية التي تستخدم المنفذ
npx kill-port 3000

# أو استخدم منفذ آخر
npm run dev -- -p 3001
```

---

### المشكلة 4: الموقع لا يفتح

**الحلول:**

1. **تحقق من أن الخادم يعمل:**
   ```bash
   # يجب أن ترى:
   # ✓ Ready in X ms
   # ○ Local: http://localhost:3000
   ```

2. **جرّب متصفح آخر:**
   - Chrome
   - Firefox
   - Edge

3. **امسح ذاكرة المتصفح:**
   - Ctrl + Shift + Delete
   - امسح Cache and Cookies

---

### المشكلة 5: الحركات لا تعمل

**الحلول:**

1. **أعد تحميل الصفحة:**
   ```
   Ctrl + Shift + R (Windows)
   Cmd + Shift + R (Mac)
   ```

2. **تأكد من JavaScript مفعّل:**
   - إعدادات المتصفح → Privacy → JavaScript

3. **تحقق من Console للأخطاء:**
   - اضغط F12
   - تبويب Console
   - هل هناك أخطاء حمراء؟

---

## ✏️ تخصيص المحتوى

### تعديل النصوص

#### مثال: تغيير عنوان الشريحة الأولى

1. **افتح الملف:**
   ```
   app/slides/1/page.tsx
   ```

2. **ابحث عن:**
   ```typescript
   <h1 className="text-6xl md:text-8xl font-black text-gradient mb-4">
     المقدمة
   </h1>
   ```

3. **غيّر إلى:**
   ```typescript
   <h1 className="text-6xl md:text-8xl font-black text-gradient mb-4">
     مشروعي الخاص
   </h1>
   ```

4. **احفظ الملف** (Ctrl + S)

5. **الموقع سيحدّث تلقائياً!** ⚡

---

### تغيير الألوان

#### مثال: تغيير اللون الأساسي

1. **افتح:**
   ```
   app/globals.css
   ```

2. **ابحث عن:**
   ```css
   :root {
     --primary: 217.2 91.2% 59.8%;
   ```

3. **غيّر إلى لون آخر:**
   ```css
   :root {
     --primary: 142 76% 36%;  /* أخضر */
     /* أو */
     --primary: 346 84% 61%;  /* زهري */
     /* أو */
     --primary: 262 83% 58%;  /* بنفسجي */
   ```

---

### تغيير خلفية الشريحة

1. **افتح أي شريحة** (مثلاً `app/slides/1/page.tsx`)

2. **ابحث عن:**
   ```typescript
   <Slide background="gradient1">
   ```

3. **جرّب خلفيات أخرى:**
   ```typescript
   <Slide background="gradient2">  // أزرق
   <Slide background="gradient3">  // بنفسجي
   <Slide background="gradient4">  // وردي
   <Slide background="gradient5">  // رمادي
   <Slide background="dark">       // أسود
   ```

---

## 📚 الملفات المهمة

| الملف | الغرض | متى تعدّله؟ |
|-------|-------|-------------|
| `app/slides/1/page.tsx` | الشريحة الأولى | لتغيير المحتوى |
| `app/slides/*/page.tsx` | باقي الشرائح | لتغيير المحتوى |
| `app/globals.css` | الألوان والأنماط | لتغيير الشكل العام |
| `package.json` | التبعيات | نادراً |
| `next.config.js` | إعدادات المشروع | للنشر فقط |

---

## 🎨 أمثلة عملية

### مثال 1: إضافة شعار

1. **ضع صورة الشعار في:**
   ```
   public/logo.png
   ```

2. **عدّل `app/slides/1/page.tsx`:**
   ```typescript
   import Image from 'next/image'
   
   // داخل المكون
   <Image 
     src="/logo.png" 
     alt="الشعار" 
     width={200} 
     height={200}
   />
   ```

---

### مثال 2: تغيير سرعة الحركة

**في أي Topic:**
```typescript
<Topic delay={0.3}>      // سريع
<Topic delay={0.6}>      // متوسط
<Topic delay={1.2}>      // بطيء
```

---

### مثال 3: إخفاء النقاط الجانبية

**في `app/slides/layout.tsx`:**
```typescript
// احذف أو علّق هذا السطر:
// <NavigationDots />
```

---

## 🚀 الخطوات التالية

### للمبتدئين:
1. ✅ جرّب تعديل النصوص
2. ✅ غيّر الألوان
3. ✅ جرّب الخلفيات المختلفة
4. ✅ اقرأ `QUICKSTART.md`

### للمتقدمين:
1. ✅ أضف شرائح جديدة
2. ✅ أنشئ مكونات مخصصة
3. ✅ عدّل الحركات
4. ✅ اقرأ `README.md`

### للنشر:
1. ✅ اختبر على أجهزة مختلفة
2. ✅ راجع جميع الشرائح
3. ✅ اقرأ `DEPLOYMENT.md`
4. ✅ انشر على Vercel أو GitHub Pages

---

## 📖 المصادر التعليمية

### الأدلة المتوفرة:
- 📘 `README.md` - الدليل الشامل
- ⚡ `QUICKSTART.md` - البدء السريع
- 🚀 `DEPLOYMENT.md` - دليل النشر
- 📊 `PROJECT_SUMMARY.md` - ملخص المشروع

### روابط مفيدة:
- [Next.js Docs](https://nextjs.org/docs)
- [Framer Motion Docs](https://www.framer.com/motion/)
- [Tailwind CSS Docs](https://tailwindcss.com/docs)
- [GSAP Docs](https://greensock.com/docs/)

---

## ❓ الأسئلة الشائعة

### س: هل يعمل على الموبايل؟
✅ نعم! المشروع متجاوب تماماً.

### س: هل يمكن تغيير اللغة للإنجليزية؟
✅ نعم، عدّل النصوص وغيّر `dir="rtl"` إلى `dir="ltr"` في `app/layout.tsx`

### س: كيف أضيف شريحة جديدة؟
✅ أنشئ مجلد جديد في `app/slides/11/` وملف `page.tsx`

### س: هل يمكن استخدامه لمشاريع تجارية؟
✅ نعم، المشروع مفتوح المصدر.

---

## 🆘 تحتاج مساعدة؟

### خيارات الدعم:

1. **اقرأ الأدلة:**
   - `README.md`
   - `QUICKSTART.md`
   - `DEPLOYMENT.md`

2. **تحقق من الأخطاء:**
   ```bash
   npm run verify
   npm run lint
   ```

3. **ابحث عن حلول:**
   - راجع قسم "حل المشاكل" أعلاه
   - تحقق من Console في المتصفح (F12)

4. **اطلب المساعدة:**
   - افتح Issue على GitHub
   - راسلنا: info@project.com

---

## ✅ قائمة التحقق النهائية

قبل أن تبدأ العمل الجدي، تحقق من:

- [ ] Node.js مثبت (`node --version`)
- [ ] npm يعمل (`npm --version`)
- [ ] التبعيات مثبتة (`npm install`)
- [ ] المشروع يعمل (`npm run dev`)
- [ ] الموقع يفتح (http://localhost:3000)
- [ ] التنقل يعمل (الأسهم، النقاط)
- [ ] الحركات تظهر
- [ ] قرأت على الأقل `QUICKSTART.md`

---

## 🎉 تهانينا!

أنت الآن جاهز لإنشاء عروض تقديمية رائعة!

**استمتع بالإبداع! 🚀✨**

---

**آخر تحديث:** 2025
**الإصدار:** 1.0.0

