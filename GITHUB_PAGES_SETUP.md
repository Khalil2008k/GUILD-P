# 🚀 إعداد GitHub Pages للنشر

## الخطوات المطلوبة:

### 1. إنشاء مستودع على GitHub

1. اذهب إلى [GitHub.com](https://github.com) وسجّل دخولك
2. انقر على زر **"New repository"** (أو "+" → "New repository")
3. اسم المستودع: `presentation` (أو أي اسم تريده)
4. اختر **Public** أو **Private** (Public يسمح بالوصول المجاني)
5. **لا** تضع علامة على "Initialize this repository with a README" (لأنك لديك ملفات بالفعل)
6. انقر على **"Create repository"**

### 2. ربط المستودع المحلي بـ GitHub

افتح Terminal في مجلد المشروع واكتب:

```bash
# أضف المستودع البعيد (استبدل YOUR_USERNAME باسم المستخدم الخاص بك)
git remote add origin https://github.com/YOUR_USERNAME/presentation.git

# ادفع الملفات إلى GitHub
git branch -M main
git push -u origin main
```

### 3. تفعيل GitHub Pages

1. اذهب إلى المستودع على GitHub
2. انقر على **Settings** (الإعدادات)
3. في القائمة الجانبية، انقر على **Pages**
4. تحت **Source**، اختر **GitHub Actions**
5. احفظ التغييرات

### 5. تحديث basePath (إذا كان اسم المستودع ليس "presentation")

إذا كان اسم المستودع مختلفًا عن "presentation"، قم بتحديث ملف `.github/workflows/deploy.yml`:

في السطر الذي يحتوي على `NEXT_PUBLIC_BASE_PATH`، استبدل `/presentation` باسم المستودع الخاص بك:

```yaml
NEXT_PUBLIC_BASE_PATH: '/your-repo-name'
```

**أو إذا كنت تستخدم username.github.io (مستودع خاص للصفحة الرئيسية):**
اتركه فارغًا: `NEXT_PUBLIC_BASE_PATH: ''`

### 6. الانتظار لنشر الموقع

بعد رفع التغييرات:

1. اذهب إلى تبويب **Actions** في GitHub
2. ستجد workflow قيد التشغيل اسمه "Deploy to GitHub Pages"
3. انتظر حتى يكتمل (عادة 2-5 دقائق)
4. بعد اكتماله، ستجد رابط الموقع في تبويب **Settings** → **Pages**

### 7. الوصول إلى العرض التقديمي

بعد اكتمال النشر، سيكون الرابط:

```
https://YOUR_USERNAME.github.io/presentation/
```

أو إذا كان اسم المستودع مختلفًا:
```
https://YOUR_USERNAME.github.io/YOUR_REPO_NAME/
```

**ملاحظة:** إذا استخدمت مستودع `username.github.io`، سيكون الرابط:
```
https://YOUR_USERNAME.github.io/
```

---

## 🔄 تحديث العرض التقديمي

في أي وقت تريد تحديث العرض:

```bash
git add .
git commit -m "Update presentation"
git push
```

سيتم تحديث الموقع تلقائيًا خلال 2-5 دقائق.

---

## 📝 ملاحظات مهمة

- **التحذيرات حول LF/CRLF:** هذه طبيعية على Windows ولا تؤثر على عمل الموقع
- **الوقت الأولي للنشر:** قد يستغرق 5-10 دقائق في المرة الأولى
- **التحديثات اللاحقة:** عادة 2-5 دقائق
- **المجال المجاني:** GitHub Pages يقدم استضافة مجانية مع نطاق `.github.io`

---

## 🎯 إذا واجهت مشاكل

1. **الموقع لا يظهر:** تحقق من تبويب **Actions** للتأكد من نجاح العملية
2. **الصور لا تعمل:** تأكد من أن `images.unoptimized: true` في `next.config.js`
3. **الروابط لا تعمل:** تأكد من أن `basePath` صحيح في workflow

---

**تم التهيئة! 🎉**

